import * as React from 'react';
// import TaskList from './components/TaskList';

const App = () => {
  return (
    <div>
      {/* <p>Hello, {count} fois.</p>
      <button onClick={() => setCount(count + 1)}>Incrémenter</button> */}
      {/* <TaskList /> */}
      <h1>Hello</h1>
    </div>
  );
}

export default App;